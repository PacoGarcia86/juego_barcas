# Solo desarrollo local. Copia a `.env` — `.env` nunca se commitea.
#
# El juego es 100 % cliente: la regata, el astillero y el progreso viven en el
# navegador (SPEC-005 `P-1xx`). No hace falta ninguna clave para jugar ni para
# construir. Este fichero existe para el día que haya partidas en la nube.

# FIREBASE_TOKEN: solo lo usa el despliegue en CI, desde los secretos de
# GitHub. Nunca desde un fichero. Ver DEPLOYMENT.md.
# FIREBASE_TOKEN=""
